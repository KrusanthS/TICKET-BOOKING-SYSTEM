{
  "name": "ticket-system-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && NODE_ENV=production npm run build --prefix frontend",
    "start": "NODE_ENV=production node backend/server.js",
    "dev-backend": "cd backend; npm run dev",
    "dev-frontend": "cd frontend; npm run dev"
  }
}
